---
defaults:
- paths: example
- files: example

# number of GPUs per node and number of nodes (for DDP)
accelerator: auto
num_gpus_per_node:  1 # use slurm for DDP
num_nodes: 1
num_gpus_per_model: 1
